interp.c
========

This file contains routines for the initialization and finalization of the
TinyLISP interpreter state; by far, its largest content is the initialization
of `top_env`, the environment used by all TinyLISP applications.

.. doxygenfile:: interp.c
